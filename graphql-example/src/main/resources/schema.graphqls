type Query {
	book(id: ID): Book
	books: [Book]
}

type Book {
  id: ID
  title: String
  author: Author
}

type Author {
  id: ID
  name: String
}

#type Mutation {
#	category(name: String): Category
#}