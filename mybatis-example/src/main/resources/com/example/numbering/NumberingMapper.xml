<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.numbering.NumberingMapper">

	<select id="nextval" resultType="_long">
		select nextval(#{name})
	</select>

</mapper>