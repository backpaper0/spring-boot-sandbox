<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.types.TypesMapper">

	<select id="selectById" resultType="com.example.types.Model4">
		select
			*
		from
			table4
		where
			id = #{value}
	</select>

</mapper>