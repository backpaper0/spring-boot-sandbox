Spring Boot 2.0.0.RELEASEからJacksonのデフォルトの日付フォーマットが不便ではなくなっていた。

```
mvn clean compile spring-boot:run -Dspring-boot.version=1.5.22.RELEASE
mvn clean compile spring-boot:run -Dspring-boot.version=2.0.0.RELEASE
```
